import Header from './components/Header'
function App() {
  const name = "Corey"
  const x =true
  return (

    <div className="container">
      <h1> Hello from react, khorally </h1>

      <h2> you so fiiiinnneee { x ? 'yes': 'no'} </h2>
    </div>

  );
}

export default App;
